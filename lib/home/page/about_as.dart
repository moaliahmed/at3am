import 'package:flutter/material.dart';
class AboutAsScreen extends StatelessWidget {
  const AboutAsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
